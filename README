Dijkstra algorithm is implemented as the optimal route finder. Students are responsible to implement the "server" folder (except for the txt data file). The client and map are provided in the class.

Running instruction:
0. Download GNOME Terminal: https://linux.softpedia.com/get/Terminals/gnome-terminal-12210.shtml (this allows us to run a command on a  newly-opened terminal 
												  from the original terminal)
1. Go to /soln/server using terminal
2. run the "make" command
3. Go back to main branch
4. run the "make" command
5. Click 2 points on the screen to choose the positions you want to find a route.
6. Close the machine by click the x button on the top right
